"""Learning — self-improvement and Hinton-style distillation.

Modules:
- ``outcome`` — Beta(alpha, beta) scoring updates, Thompson sampling
- ``distill`` — episodic -> procedural distillation
- ``replay`` — Reflexion-style failure replay
"""
