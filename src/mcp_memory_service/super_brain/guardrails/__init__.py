"""Guardrails — context + hallucination guards.

Modules:
- ``claim_gate`` — block low-provenance claims from semantic promotion
- ``crossref`` — product-of-experts multi-source verifier
- ``uncertainty`` — SelfCheckGPT-style self-consistency score
"""
