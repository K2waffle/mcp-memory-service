"""Payments — revenue event recording, payment-rail agnostic.

Modules:
- ``stripe_client`` — Stripe SDK wrapper with idempotency + PCI SAQ-A wiring
- ``revenue_events`` — rail-agnostic event recording and reconciliation
"""
