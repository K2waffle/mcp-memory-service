"""Eval — continuous quality measurement and regression catching.

Modules:
- ``harness`` — shared test harness
- ``memory_agent_bench`` — MemoryAgentBench / LongMemEval wrappers
- ``ragas_wrapper`` — RAGAS / DeepEval metric wrappers
"""
