"""Legal — provenance, retention, and crypto-shredding.

Modules:
- ``provenance`` — schema validators (lawful_basis, license, attribution)
- ``retention`` — retention policy engine (min/max days by jurisdiction)
- ``crypto_shred`` — per-subject key registry + shred-on-erasure (GDPR Art. 17)
"""
